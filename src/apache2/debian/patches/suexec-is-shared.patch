Index: apache2-2.2.19/os/unix/unixd.c
===================================================================
--- apache2-2.2.19.orig/os/unix/unixd.c	2011-02-14 23:18:20.000000000 +0300
+++ apache2-2.2.19/os/unix/unixd.c	2011-05-24 16:07:09.488131311 +0400
@@ -266,6 +266,10 @@
 
     /* Check for suexec */
     unixd_config.suexec_enabled = 0;
+    /* If mod_suexec isn't linked in, we shouldn't test for the binary */
+    if (ap_find_linked_module("mod_suexec.c") == NULL) {
+	return;
+    }
     if ((apr_stat(&wrapper, SUEXEC_BIN,
                   APR_FINFO_NORM, ptemp)) != APR_SUCCESS) {
         return;
