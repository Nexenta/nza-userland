#!/bin/sh

set -e

if [ "x$1" = xconfigure ]; then
        useradd -d  /var/run/c-icap c-icap
        chown c-icap /var/log/c-icap
	mkdir -p /var/run/c-icap
	chown c-icap /var/run/c-icap
	            
fi

#DEBHELPER#

exit 0
