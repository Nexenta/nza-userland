#!/bin/sh -e

if [ "$1" = "configure" ] || [ "$1" = "upgrade" ]; then
  update-alternatives --install /usr/gnu/bin/infobrowser infobrowser \
                                /usr/gnu/bin/info 60 
#                      --slave /usr/gnu/share/man/man1/infobrowser.1.gz infobrowser.1.gz \
#                              /usr/gnu/share/man/man1/info.1.gz
fi

#DEBHELPER#
