#!/bin/sh -e

case "$1" in
    remove)
        # This link was created by SMF method
        # and may point to /usr/sbin/apache2-{worker,prefork,event}
        if [ -L /usr/sbin/apache2 ]; then
            rm /usr/sbin/apache2 || true
        fi
        ;;
esac

#DEBHELPER#
