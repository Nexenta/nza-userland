case "$1" in
    install|upgrade)
        for f in #BINARIES#
        do
            echo "Creating isaexec hard link to $f ..."
            /usr/bin/ln /usr/lib/isaexec $f || true
        done
        ;;
esac
