case "$1" in
    install|upgrade)
        for f in #BINARIES# ; do
            # On upgrade create only new files
            if [ ! -f $f ]; then
                echo "Creating isaexec hard link to $f ..."
                /usr/bin/ln /usr/lib/isaexec $f || true
            fi
        done
        ;;
esac
